name: Release plugin

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Test
        run: dotnet test tests/BTCPayServer.Plugins.MakePay.DigitalProducts.Tests/BTCPayServer.Plugins.MakePay.DigitalProducts.Tests.csproj --configuration Release -p:RazorCompileOnBuild=true --verbosity normal -m:1
      - name: Publish
        run: |
          dotnet publish src/BTCPayServer.Plugins.MakePay.DigitalProducts/BTCPayServer.Plugins.MakePay.DigitalProducts.csproj --configuration Release -p:RazorCompileOnBuild=true --no-restore --output artifacts/plugin -m:1
          cp packaging/BTCPayServer.Plugins.MakePay.DigitalProducts.json artifacts/plugin/
          cd artifacts/plugin
          zip -9 ../BTCPayServer.Plugins.MakePay.DigitalProducts.btcpay *
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/BTCPayServer.Plugins.MakePay.DigitalProducts.btcpay
